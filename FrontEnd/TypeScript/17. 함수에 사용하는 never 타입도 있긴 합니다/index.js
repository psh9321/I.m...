"use strict";
function QQQ() {
    throw new Error(); /** 실행이 중단됨 */
    /** 반복문 잘못돌렸을때 */
}
/** never 타입은 보통 void 로 대체 됨 */
function WWW(param) {
    if (typeof param === "string") {
        console.log(param);
    }
    else {
        console.log(param);
    }
}
